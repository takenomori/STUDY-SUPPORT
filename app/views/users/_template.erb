<%# サイドバー %>

  <div class="field row">
  <%= link_to user_path(current_user.id) do %>
    <%= attachment_image_tag @user, :image, format: 'jpg', fallback: "no-image.gif", size:'50' %>
  <% end %>
  </div>

  <div class="field row">
    <%= link_to user_path(current_user.id) do %>
      <%= @user.last_name %>  <%= @user.first_name %> 
    <% end %>
  </div>

  <div class="field row">
    <%= @user.email %>
  </div>
