<!-- 教材 一覧 -->
<div class="container">
	<!-- ページネーション 上部 gem kaminari-->
	<div class="row">
		<div class="text-center">
			<%= paginate(@textbooks) %>
		</div>
	</div>

	<!-- 一覧表示 -->
	<div class="row">
		<%= render 'textbooks/textbook_box' %>
		<br>
	</div>

	<!-- ページネーション 下部 -->
	<div class="row">
		<div class="text-center">
			<%= paginate(@textbooks) %>
		</div>
	</div>

	<!-- 追加ボタン -->
	<div class="row">
		<%= link_to '教材を追加', new_textbook_path, class:"btn btn-success form-control" %>	
	</div>
</div>
